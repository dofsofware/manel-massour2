import { ChangerpipePipe } from './changerpipe.pipe';

describe('ChangerpipePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangerpipePipe();
    expect(pipe).toBeTruthy();
  });
});
